# Do not remove this line or mod_rewrite rules and search engine friendly URLs will stop working


DirectoryIndex mobile-pc.php

<IfModule mod_rewrite.c>
 
    RewriteEngine on

    RewriteRule ^([^/.]+)$ login.php?notif=$1 [NC]
     
</IfModule>